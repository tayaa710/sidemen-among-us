# Port the API listens on (defaults to 3001)
PORT=3001

# YouTube Data API v3 key. Optional: without it the server serves the
# committed snapshot in videoData.json instead of refreshing from YouTube.
# Create one at https://console.cloud.google.com/apis/credentials
APIKEY=
